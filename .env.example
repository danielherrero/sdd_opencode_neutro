# Copy to .env and replace the value before running Docker Compose.
DB_PASSWORD=replace-with-a-local-password
POSTGRES_MCP_PASSWORD=replace-with-a-local-password
POSTGRES_MCP_URL=postgresql://admin:replace-with-a-local-password@localhost:5432/sdd
CONTEXT7_API_KEY=replace-with-your-context7-key
